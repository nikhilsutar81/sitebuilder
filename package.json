{
  "name": "sitebuilder-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npx prisma generate --schema=prisma/schema.prisma",
    "vercel-build": "cd server && npx prisma generate --schema=prisma/schema.prisma && npm run build --prefix server",
    "build": "npm run build --prefix server"
  }
}
