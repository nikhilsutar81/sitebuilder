{
  "version": 2,
  "projects": [
    {
      "name": "client",
      "rootDirectory": "client",
      "buildCommand": "npm run build",
      "outputDirectory": "dist",
      "framework": "vite"
    },
    {
      "name": "server",
      "rootDirectory": "server",
      "buildCommand": "npm run build",
      "outputDirectory": ".",
      "framework": "nodejs"
    }
  ]
}
